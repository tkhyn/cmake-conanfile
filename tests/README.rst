cmake-conanfile tests
#####################

To run the tests (exact commands may differ depending on platform / os):

.. code-block:: bash

   cd tests
   python3 -m venv venv
   ./venv/bin/activate
   pip install pytest

   pytest
